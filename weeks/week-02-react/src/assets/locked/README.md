# Locked assets

Drop image files here (png/jpg/jpeg/svg/webp/gif) to make them show up
automatically in the Assets tab's "tech@nyu Library" grid, alongside the
existing placeholder entries — no code changes needed.

The card name is generated from the filename, so name the file the way you
want it to read, e.g. `sponsor-logo.png` → "sponsor logo".
